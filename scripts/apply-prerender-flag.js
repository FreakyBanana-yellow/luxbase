
// No-op helper: ensures prebuild doesn't fail if file is missing.
// You can extend this to mutate frontmatter or config if needed.
console.log("[prebuild] apply-prerender-flag.js: nothing to do.");
process.exit(0);
